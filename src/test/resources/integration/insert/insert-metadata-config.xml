<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns:db="http://www.mulesoft.org/schema/mule/db"
      xmlns="http://www.mulesoft.org/schema/mule/core"
      xsi:schemaLocation="http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
            http://www.mulesoft.org/schema/mule/db http://www.mulesoft.org/schema/mule/db/current/mule-db.xsd">

    <flow name="insertOutputMetadata">
        <db:insert config-ref="dbConfig">
            <db:sql>INSERT INTO PLANET(POSITION, NAME) VALUES (777, 'Mercury')</db:sql>
        </db:insert>
    </flow>

    <flow name="bulkInsertOutputMetadata">
        <db:bulk-insert config-ref="dbConfig">
            <db:sql>INSERT INTO PLANET(POSITION, NAME) VALUES (777, :name)</db:sql>
        </db:bulk-insert>
    </flow>

    <flow name="bulkInsertNoParametersInputMetadata">
        <db:bulk-insert config-ref="dbConfig">
            <db:sql>INSERT INTO PLANET(POSITION, NAME) VALUES (777, 'Mercury')</db:sql>
        </db:bulk-insert>
    </flow>

    <flow name="bulkInsertParameterizedInputMetadata">
        <db:bulk-insert config-ref="dbConfig">
            <db:sql>INSERT INTO PLANET(POSITION, NAME) VALUES (777, :name)</db:sql>
        </db:bulk-insert>
    </flow>

    <flow name="bulkInsertParameterizedInputMetadataOracle">
        <db:bulk-insert config-ref="dbConfig">
            <db:sql>INSERT INTO PLANET(NAME) VALUES (:name)</db:sql>
        </db:bulk-insert>
    </flow>

    <flow name="insertNoParametersInputMetadata">
        <db:insert config-ref="dbConfig">
            <db:sql>INSERT INTO PLANET(POSITION, NAME) VALUES (777, 'Mercury')</db:sql>
        </db:insert>
    </flow>

    <flow name="insertParameterizedInputMetadata">
        <db:insert config-ref="dbConfig">
            <db:sql>INSERT INTO PLANET(POSITION, NAME) VALUES (777, :name)</db:sql>
        </db:insert>
    </flow>

    <flow name="insertParameterizedInputMetadataOracle">
        <db:insert config-ref="dbConfig">
            <db:sql>INSERT INTO PLANET(NAME) VALUES (:name)</db:sql>
        </db:insert>
    </flow>

    <flow name="insertWithExpressionInputMetadata">
        <db:insert config-ref="dbConfig">
            <db:sql>INSERT INTO PLANET(POSITION, NAME) VALUES (777, #[mel:payload])</db:sql>
        </db:insert>
    </flow>

</mule>
