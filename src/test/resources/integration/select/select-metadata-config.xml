<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns:db="http://www.mulesoft.org/schema/mule/db"
        xmlns="http://www.mulesoft.org/schema/mule/core"
        xsi:schemaLocation="http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
            http://www.mulesoft.org/schema/mule/db http://www.mulesoft.org/schema/mule/db/current/mule-db.xsd">

    <flow name="selectAll">
        <db:select config-ref="dbConfig">
            <db:sql>SELECT * FROM PLANET</db:sql>
        </db:select>
    </flow>

    <flow name="selectSome">
        <db:select config-ref="dbConfig">
            <db:sql>SELECT ID, POSITION FROM PLANET</db:sql>
        </db:select>
    </flow>

    <flow name="selectJoin">
        <db:select config-ref="dbConfig">
            <db:sql>SELECT NAME, NAME as NAME2 FROM PLANET</db:sql>
        </db:select>
    </flow>

    <flow name="selectInvalidJoin">
        <db:select config-ref="dbConfig">
            <db:sql>SELECT NAME, NAME FROM PLANET</db:sql>
        </db:select>
    </flow>

    <flow name="insert">
        <db:insert config-ref="dbConfig">
            <db:sql>INSERT INTO TABLE2 (FRUIT) VALUES (:fruit)</db:sql>
            <db:input-parameters><![CDATA[#[output application/java
---
{
	fruit : {fruitName : "Orange"} as Object {class : "estebanwasinger.Fruit"}
}]]]></db:input-parameters>
        </db:insert>
    </flow>

    <flow name="returnsNullSelectMetadataUnParameterizedQuery">
        <db:select config-ref="dbConfig">
            <db:sql>select * from PLANET</db:sql>
        </db:select>
    </flow>

    <flow name="returnsAnySelectInputMetadataFromNotSupportedParameterizedQuery">
        <db:select config-ref="dbConfig">
            <db:sql>select * from PLANET where id = #[mel:payload.id] and name = #[mel:message.outboundProperties.updateCount]</db:sql>
        </db:select>
    </flow>

    <flow name="returnsSelectInputMetadataFromBeanParameterizedQuery">
        <db:select config-ref="dbConfig">
            <db:sql>select * from PLANET where id = :id and name = :name</db:sql>
        </db:select>
    </flow>

</mule>
