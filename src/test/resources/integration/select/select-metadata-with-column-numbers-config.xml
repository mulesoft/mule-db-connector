<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns:db="http://www.mulesoft.org/schema/mule/db"
        xmlns="http://www.mulesoft.org/schema/mule/core"
        xsi:schemaLocation="http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
            http://www.mulesoft.org/schema/mule/db http://www.mulesoft.org/schema/mule/db/current/mule-db.xsd">

    <flow name="selectAllColumnsWithColumnNumbers">
        <db:select config-ref="dbConfig" useColumnNumbers="true">
            <db:sql>SELECT * FROM PLANET</db:sql>
        </db:select>
    </flow>

    <flow name="selectDuplicateColumnsWithColumnNumbers">
        <db:select config-ref="dbConfig" useColumnNumbers="true">
            <db:sql>SELECT NAME, NAME FROM PLANET</db:sql>
        </db:select>
    </flow>

    <flow name="querySingleMetadataWithColumnNumbers">
        <db:query-single config-ref="dbConfig" useColumnNumbers="true">
            <db:sql>SELECT * FROM PLANET</db:sql>
        </db:query-single>
    </flow>

</mule>